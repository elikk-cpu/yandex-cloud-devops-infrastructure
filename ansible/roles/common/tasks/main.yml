---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600
  become: true

- name: Install basic packages
  ansible.builtin.apt:
    name:
      - curl
      - ca-certificates
    state: present
  become: true
